import {WP} from '../../styles/Dimesions';
import {ROBOTO_BOLD, ROBOTO_REGULAR} from '../../styles/Fonts&Colors';

const {StyleSheet} = require('react-native');

export default StyleSheet.create({
  container: {
    flex: 1,
  },
});
